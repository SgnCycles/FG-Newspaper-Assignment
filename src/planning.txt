ARTICLES 

  --------------

App.jsx
  article state defaults to null - / full Article Object

  if (state)
    show FullArticleComponent

For every articleComponents have an onClick event:
  Pass down updateFunction
  Get name of the article
  Add it to state

  1.make a new articleComponents
  2.add it to the app.jsx
  3.add state to app.jsx
  4.update the current articles (small, medium, large)
  5.pass down the article object to the articleComponents
  6.create onClick function
  7.render the whole article in new view

  CATEGORIES
  --------------
  Data:
    Clone the articles array
    call it allArticles

  Make each of the menu buttons clickable using state

  State default to Null
    for each menu item:
      pass down the updateStatew function

      selectCategory component 
        (pass in the string as state)
        pass in an array of the articles in that category
        get all the articles in this component

So on Click A category must show category articles, and if article is clicked the article must be shown.

PLAN:
 
ARTICLES
---------
 
App.jsx:
   article state defaults to null 
   category state defaults to null
 
   if (ArticleState)
      Show FullArticleComponent = state =>
 
   if(CategoryState)
       Show selectedCategory = state =>
 
For every articleComponents have an onClick event
    Pass down updateFunction
    TRefactor so the component is passed the  entire article object -currently we are destructuring it
    Add it to state
 
CATEGORIES
-----------
   Data:
      Clone tha articles array
      call it allArticles
 
   State default to Null
 
   For each Menu item:
      Pass down the updateState function
      setState to that category string
      also pass down the updateState function for articles
      set articleState to null
 
    selectedCategory component
       pass in the string in state
       use that string to get all of the articles in that category
           AllArticle.filter(state)